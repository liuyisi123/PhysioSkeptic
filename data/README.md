# Data directory

This directory contains only schemas and toy examples. Do not commit credentialed waveform datasets, raw patient signals, protected health information, API responses containing private content, or model checkpoints derived from restricted data.

Use local paths outside the repository for MIMIC-III-Ext-PPG or MC-MED.
